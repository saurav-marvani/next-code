Replace an entire symbol (function, method, class, struct) by name using LSP document symbols to find exact boundaries. Prefer this over `edit` for whole-symbol replacements: it eliminates whitespace-matching failures by resolving symbol ranges through the language server instead of exact text matching. The replacement includes the full symbol range (signature + body). Returns diagnostics after the edit.
